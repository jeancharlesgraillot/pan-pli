    <nav class="navbar navbar-expand-lg navbar-dark bg-primary m-0 p-0">
      <a href="{{ path('homepage')}}" class="navbar-brand m-0 p-0">
        <img src="/images/logo_panpli.jpg" width="100" height="100" alt="Logo de l'association Pan Pli">
      </a>
      <a class="navbar-brand ml-3" href="{{ path('homepage')}}">Association PAN PLI</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse ml-3" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="{{ path('homepage')}}">
              <i class="mr-1 fas fa-home"></i></i>Accueil
            </a>
          </li>
          <li class="nav-item">
            <a href="{{ path('project_index')}}" class="nav-link">
              <i class="mr-1 fas fa-project-diagram"></i>Projet
            </a>
          </li>
          <li class="nav-item">
            <a href="{{ path('spiruline_index')}}" class="nav-link">
              <i class="mr-1 fab fa-product-hunt"></i>Spiruline PAN PLI
            </a>
          </li>
          <li class="nav-item">
            <a href="{{ path('blog_index')}}" class="nav-link">
              <i class="mr-1 fas fa-blog"></i>Blog
            </a>
          </li>
          {# <li class="nav-item">
            <a href="{{ path('products_index')}}" class="nav-link">
              <i class="mr-1 fab fa-product-hunt"></i>Produits
            </a>
          </li> #}

        </ul>
        <ul class="navbar-nav">
          <li class="nav-item">
            <a href="https://www.facebook.com/association.pan.pli/" target="_blank" class="nav-link d-flex align-items-center">
              <img class="mr-1" src="/images/facebook.svg" width="25" height="25" alt="Facebook PAN PLI">
              PAN PLI
            </a>
          </li>
          <li class="nav-item">
            <a href="https://www.facebook.com/fermedespossibles/" target="_blank" class="nav-link d-flex align-items-center">
              <img class="mr-1" src="/images/facebook.svg" width="25" height="25" alt="Facebook Ferme des Possibles">
              La Ferme des Possibles
            </a>
          </li>
        </ul>

        <ul class="navbar-nav ml-auto mr-3">
          {% if app.user %}
            <li class="nav-item dropdown">
              <a href="#" id="navbarDropdownMenuLink" class="nav-link dropdown-toogle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              {% if app.user.picture %}
                <img src="{{ app.user.picture }}" class="mr-1 avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}">
                {{app.user.fullName}}<i class="fas fa-arrow-down ml-2"></i>
              {% else %}
                <div class="d-flex align-items-center">
                  <div><i class="fas fa-user-circle fa-2x mr-2"></i></div>
                  <div>{{app.user.fullName}}<i class="fas fa-arrow-down ml-2"></i></div>
                </div>
              {% endif %}
              </a>

              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a href="{{ path('account_profile')}}" class="dropdown-item">Modifier mon profil</a>
                <a href="{{ path('account_password')}}" class="dropdown-item">Modifier mon mot de passe</a>
                <div class="dropdown-divider"></div>
                <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
              </div>
            </li>
          {% else %}
          <li class="nav-item">
            <a href="{{ path('account_register') }}" class="nav-link">Inscription</a>
          </li>
          <li class="nav-item">
            <a href="{{ path('account_login') }}" class="nav-link">Connexion</a>
          </li>
          {% endif %}
        </ul>
      </div>
    </nav>

    {% for label, messages in app.flashes %}
      <div class="container">
        <div class="alert alert-{{ label }}">
          {% for message in messages %}
            <p>{{ message | raw }}</p>
          {% endfor %}
        </div>
      </div>
    {% endfor %}